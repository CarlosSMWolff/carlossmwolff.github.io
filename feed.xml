<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://carlossmwolff.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://carlossmwolff.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-13T02:04:45+00:00</updated><id>https://carlossmwolff.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">creating a private audiobook server</title><link href="https://carlossmwolff.github.io/blog/2025/audiobookshelf-raspberry-pi-tutorial/" rel="alternate" type="text/html" title="creating a private audiobook server"/><published>2025-05-04T17:00:00+00:00</published><updated>2025-05-04T17:00:00+00:00</updated><id>https://carlossmwolff.github.io/blog/2025/audiobookshelf-raspberry-pi-tutorial</id><content type="html" xml:base="https://carlossmwolff.github.io/blog/2025/audiobookshelf-raspberry-pi-tutorial/"><![CDATA[<p>The following tutorial is completely written by Claude after a sucessful session of vibe-coding to setup a private audiobook server for me and my friends, which I currently serve at <a href="https://audiobooks.carlossanchezmunoz.com">audiobooks.carlossanchezmunoz.com</a>. Not a very human text, but hey, this was useful to me, and we had to fix some initial mistakes by Claude along the way, so I guess the final recollection can be pretty useful, either for my future self or anyone interested.</p> <h1 id="self-hosted-audiobook-server-with-raspberry-pi-audiobookshelf--cloudflare-tunnel">Self-hosted Audiobook Server with Raspberry Pi, Audiobookshelf &amp; Cloudflare Tunnel</h1> <p>A complete guide to building a silent, always-on audiobook server you can stream from anywhere in the world — for you, your family, or anyone you share it with. No screen, no keyboard, no subscription fees.</p> <hr/> <h2 id="what-youll-end-up-with">What you’ll end up with</h2> <ul> <li>A headless Raspberry Pi server running <strong>Audiobookshelf</strong></li> <li>Accessible from <strong>anywhere in the world</strong> via a personal domain with HTTPS</li> <li><strong>Multiple users</strong> with independent listening progress (you and your partner, for example)</li> <li>Fully controllable from your Mac via SSH — no monitor ever needed</li> <li>Streaming audio without downloading files to your phone</li> </ul> <hr/> <h2 id="what-you-need">What you need</h2> <ul> <li>Raspberry Pi 4 or 5 (2GB RAM minimum recommended)</li> <li>MicroSD card (for OS) — 16GB or more</li> <li>External USB SSD or HDD for your audiobook library (optional for testing, recommended for real use)</li> <li>A Mac to set everything up from</li> <li>A domain managed on Cloudflare (free account is enough)</li> <li>A Cloudflare account</li> </ul> <hr/> <h2 id="hardware-notes">Hardware notes</h2> <h3 id="storage">Storage</h3> <p>For a permanent setup, avoid storing your audiobook library on the microSD card — they wear out under constant read/write. A USB SSD is the best option: silent, fast, and reliable. A 1TB USB SSD costs around 60-85€ and is more than enough for thousands of audiobooks (each one is typically 200–500MB).</p> <p>If you have a <strong>Raspberry Pi 5</strong>, you can add an NVMe HAT (~20€) and a 1TB NVMe SSD (~45€) for the fastest possible setup, well within 100€.</p> <h3 id="cooling">Cooling</h3> <p>For this use case — audio streaming and light home automation — the Pi runs at very low CPU. You don’t need an active fan. A <strong>passive heatsink case</strong> (like the Flirc or Argon ONE) keeps temperatures around 50–55°C silently. Just heatsink stickers (~3€) are also enough for light workloads.</p> <hr/> <h2 id="step-1--flash-the-os-on-your-mac">Step 1 — Flash the OS on your Mac</h2> <p>Download and install <strong>Raspberry Pi Imager</strong> from <a href="https://www.raspberrypi.com/software/">raspberrypi.com</a>.</p> <ol> <li>Open Imager → <strong>Choose OS</strong> → <em>Raspberry Pi OS Lite (64-bit)</em> — no desktop environment needed, saves resources</li> <li><strong>Choose Storage</strong> → select your SD card</li> <li>Click the <strong>gear icon ⚙️</strong> before writing — this is critical for headless setup: <ul> <li><strong>Hostname</strong>: <code class="language-plaintext highlighter-rouge">pi-home</code> (or whatever you prefer)</li> <li><strong>Enable SSH</strong> ✅</li> <li><strong>Username</strong>: <code class="language-plaintext highlighter-rouge">carlos</code> (use your own name)</li> <li><strong>Password</strong>: choose a strong one</li> <li><strong>WiFi</strong>: enter your network credentials — or skip if using ethernet (recommended)</li> <li><strong>Locale / Timezone</strong>: set to your region</li> </ul> </li> <li>Write the image</li> </ol> <blockquote> <p><strong>Tip:</strong> Ethernet is better than WiFi for a permanent home server — more stable, faster file transfers, less CPU overhead, and one less thing that can break.</p> </blockquote> <hr/> <h2 id="step-2--first-boot">Step 2 — First boot</h2> <ul> <li>Insert the SD card into the Pi</li> <li>Connect ethernet cable if using wired connection</li> <li>Power on the Pi</li> <li>Wait 2 minutes for the first boot to complete</li> </ul> <hr/> <h2 id="step-3--connect-from-your-mac-via-ssh">Step 3 — Connect from your Mac via SSH</h2> <p>Open <strong>Terminal</strong> on your Mac:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh carlos@pi-home.local
</code></pre></div></div> <p>If that doesn’t resolve, find the Pi’s IP from your router admin panel and use:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh carlos@192.168.1.XXX
</code></pre></div></div> <p>You’re now controlling the Pi from your Mac. You’ll never need a screen or keyboard.</p> <h3 id="optional-passwordless-ssh">Optional: passwordless SSH</h3> <p>Save yourself from typing the password every time:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-copy-id carlos@pi-home.local
</code></pre></div></div> <h3 id="optional-keep-ssh-sessions-alive">Optional: keep SSH sessions alive</h3> <p>Add this to <code class="language-plaintext highlighter-rouge">~/.ssh/config</code> on your Mac:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host pi-home.local
  ServerAliveInterval 60
</code></pre></div></div> <hr/> <h2 id="step-4--update-the-system">Step 4 — Update the system</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt update <span class="o">&amp;&amp;</span> <span class="nb">sudo </span>apt upgrade <span class="nt">-y</span>
</code></pre></div></div> <hr/> <h2 id="step-5--mount-your-external-drive-skip-for-testing">Step 5 — Mount your external drive (skip for testing)</h2> <p>If you’re just testing with the SD card, skip this step and come back to it when you’re ready for a permanent setup.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Find the drive name (usually /dev/sda1)</span>
lsblk

<span class="c"># Create mount point</span>
<span class="nb">sudo mkdir</span> /mnt/audiobooks

<span class="c"># Format if brand new</span>
<span class="nb">sudo </span>mkfs.ext4 /dev/sda1

<span class="c"># Mount it</span>
<span class="nb">sudo </span>mount /dev/sda1 /mnt/audiobooks

<span class="c"># Auto-mount on reboot</span>
<span class="nb">echo</span> <span class="s1">'/dev/sda1 /mnt/audiobooks ext4 defaults,nofail 0 2'</span> | <span class="nb">sudo tee</span> <span class="nt">-a</span> /etc/fstab
</code></pre></div></div> <blockquote> <p>If testing with SD card only, Audiobookshelf will store files at <code class="language-plaintext highlighter-rouge">/home/carlos/audiobooks</code> instead.</p> </blockquote> <hr/> <h2 id="step-6--install-docker-and-run-audiobookshelf">Step 6 — Install Docker and run Audiobookshelf</h2> <p>Docker is the easiest way to run Audiobookshelf — no dependency management, easy updates, clean removal.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Install Docker</span>
curl <span class="nt">-fsSL</span> https://get.docker.com | sh
<span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker carlos
</code></pre></div></div> <p>Log out and back in so the group change takes effect:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">exit</span>
<span class="c"># SSH back in, then:</span>

docker run <span class="nt">-d</span> <span class="se">\</span>
  <span class="nt">--name</span> audiobookshelf <span class="se">\</span>
  <span class="nt">--restart</span> unless-stopped <span class="se">\</span>
  <span class="nt">-p</span> 13378:80 <span class="se">\</span>
  <span class="nt">-v</span> /home/carlos/audiobooks:/audiobooks <span class="se">\</span>
  <span class="nt">-v</span> /home/carlos/abs/config:/config <span class="se">\</span>
  <span class="nt">-v</span> /home/carlos/abs/metadata:/metadata <span class="se">\</span>
  ghcr.io/advplyr/audiobookshelf
</code></pre></div></div> <blockquote> <p>If using external drive, replace <code class="language-plaintext highlighter-rouge">/home/carlos/audiobooks</code> with <code class="language-plaintext highlighter-rouge">/mnt/audiobooks</code>.</p> </blockquote> <hr/> <h2 id="step-7--access-audiobookshelf-on-your-local-network">Step 7 — Access Audiobookshelf on your local network</h2> <p>Open your browser on any device connected to your home WiFi:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://pi-home.local:13378
</code></pre></div></div> <p>Or using the IP directly:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://192.168.1.XXX:13378
</code></pre></div></div> <p>Find the IP anytime with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">hostname</span> <span class="nt">-I</span>
</code></pre></div></div> <p>Create your admin account on first access and set the library path to <code class="language-plaintext highlighter-rouge">/audiobooks</code>.</p> <hr/> <h2 id="step-8--set-up-multiple-users">Step 8 — Set up multiple users</h2> <p>Audiobookshelf has full multi-user support built in. Each user gets completely independent listening progress, bookmarks, history, and preferences — sharing the same library.</p> <p>Go to: <strong>Settings → Users → Create User</strong></p> <p>Give each family member their own username and password. They log in on their phone with their own account and everything is tracked separately.</p> <hr/> <h2 id="step-9--copy-audiobooks-to-the-pi">Step 9 — Copy audiobooks to the Pi</h2> <p>The easiest way on Mac is <strong>Cyberduck</strong> — free, works like Finder, connects via SFTP.</p> <ol> <li>Download <a href="https://cyberduck.io">Cyberduck</a> (free)</li> <li>New Connection → <strong>SFTP</strong></li> <li>Server: <code class="language-plaintext highlighter-rouge">pi-home.local</code>, Port: <code class="language-plaintext highlighter-rouge">22</code>, Username: <code class="language-plaintext highlighter-rouge">carlos</code>, Password: your Pi password</li> <li>Drag and drop your audiobook files to <code class="language-plaintext highlighter-rouge">/home/carlos/audiobooks</code></li> </ol> <h3 id="fix-permissions-if-needed">Fix permissions if needed</h3> <p>If Cyberduck shows “permission denied”:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo chown</span> <span class="nt">-R</span> carlos:carlos /home/carlos/audiobooks
</code></pre></div></div> <h3 id="alternative-mount-as-finder-network-drive">Alternative: mount as Finder network drive</h3> <p>In Finder → <strong>Go → Connect to Server</strong> (⌘K) → enter <code class="language-plaintext highlighter-rouge">sftp://carlos@pi-home.local</code></p> <h3 id="after-copying-files">After copying files</h3> <p>In Audiobookshelf → <strong>Libraries</strong> → click <strong>Scan</strong> to pick up new files.</p> <hr/> <h2 id="step-10--expose-to-the-internet-with-cloudflare-tunnel">Step 10 — Expose to the internet with Cloudflare Tunnel</h2> <p>This lets you and anyone you share it with stream from anywhere in the world, with automatic HTTPS — no port forwarding, no VPN required.</p> <p><strong>Prerequisites:</strong> Your domain must be managed on Cloudflare (free account).</p> <h3 id="install-cloudflared">Install cloudflared</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-L</span> https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-arm64 <span class="nt">-o</span> cloudflared
<span class="nb">sudo mv </span>cloudflared /usr/local/bin/
<span class="nb">sudo chmod</span> +x /usr/local/bin/cloudflared
</code></pre></div></div> <h3 id="authenticate-with-cloudflare">Authenticate with Cloudflare</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cloudflared tunnel login
</code></pre></div></div> <p>It prints a URL — open it on your Mac, select your domain, and authorize it.</p> <h3 id="create-the-tunnel">Create the tunnel</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cloudflared tunnel create audiobooks
</code></pre></div></div> <p>Note the <strong>tunnel ID</strong> it returns (a UUID string).</p> <h3 id="create-the-config-file">Create the config file</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/cloudflared
<span class="nb">sudo </span>nano /etc/cloudflared/config.yml
</code></pre></div></div> <p>Paste this (replace with your tunnel ID and domain):</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">tunnel</span><span class="pi">:</span> <span class="s">YOUR-TUNNEL-ID</span>
<span class="na">credentials-file</span><span class="pi">:</span> <span class="s">/etc/cloudflared/YOUR-TUNNEL-ID.json</span>

<span class="na">ingress</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">hostname</span><span class="pi">:</span> <span class="s">audiobooks.yourdomain.com</span>
    <span class="na">service</span><span class="pi">:</span> <span class="s">http://localhost:13378</span>
  <span class="pi">-</span> <span class="na">service</span><span class="pi">:</span> <span class="s">http_status:404</span>
</code></pre></div></div> <h3 id="copy-credentials-to-system-path">Copy credentials to system path</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo cp</span> ~/.cloudflared/<span class="k">*</span>.json /etc/cloudflared/
</code></pre></div></div> <h3 id="add-dns-record">Add DNS record</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cloudflared tunnel route dns audiobooks audiobooks.yourdomain.com
</code></pre></div></div> <p>This automatically creates the CNAME in Cloudflare — no dashboard needed.</p> <h3 id="test-it">Test it</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>cloudflared tunnel run audiobooks
</code></pre></div></div> <p>Open <code class="language-plaintext highlighter-rouge">https://audiobooks.yourdomain.com</code> — it should work with HTTPS automatically.</p> <h3 id="run-on-boot">Run on boot</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>cloudflared service <span class="nb">install
sudo </span>systemctl <span class="nb">enable </span>cloudflared
<span class="nb">sudo </span>systemctl start cloudflared
</code></pre></div></div> <p>Check it’s running:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status cloudflared
</code></pre></div></div> <hr/> <h2 id="step-11--install-the-mobile-app">Step 11 — Install the mobile app</h2> <p>Audiobookshelf streams audio directly — no need to download files to your phone.</p> <p><strong>iOS options:</strong></p> <ul> <li><strong>Still</strong> — clean, minimalist, native iOS, available on the App Store</li> <li><strong>SoundLeaf</strong> — premium feel, CarPlay support, App Store</li> <li><strong>ShelfPlayer</strong> — open source, feature-rich, App Store</li> </ul> <p><strong>Android:</strong></p> <ul> <li>Official Audiobookshelf app (available on the Play Store / direct APK)</li> </ul> <p>On first launch, enter your server URL:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://audiobooks.yourdomain.com
</code></pre></div></div> <p>Then log in with your Audiobookshelf username and password.</p> <hr/> <h2 id="quick-reference">Quick reference</h2> <table> <thead> <tr> <th>Task</th> <th>Command / URL</th> </tr> </thead> <tbody> <tr> <td>SSH into Pi</td> <td><code class="language-plaintext highlighter-rouge">ssh carlos@pi-home.local</code></td> </tr> <tr> <td>Find Pi IP</td> <td><code class="language-plaintext highlighter-rouge">hostname -I</code></td> </tr> <tr> <td>Local access</td> <td><code class="language-plaintext highlighter-rouge">http://pi-home.local:13378</code></td> </tr> <tr> <td>Remote access</td> <td><code class="language-plaintext highlighter-rouge">https://audiobooks.yourdomain.com</code></td> </tr> <tr> <td>Check Docker containers</td> <td><code class="language-plaintext highlighter-rouge">docker ps</code></td> </tr> <tr> <td>Restart Audiobookshelf</td> <td><code class="language-plaintext highlighter-rouge">docker restart audiobookshelf</code></td> </tr> <tr> <td>Update Audiobookshelf</td> <td><code class="language-plaintext highlighter-rouge">docker pull ghcr.io/advplyr/audiobookshelf &amp;&amp; docker restart audiobookshelf</code></td> </tr> <tr> <td>Check Cloudflare tunnel</td> <td><code class="language-plaintext highlighter-rouge">sudo systemctl status cloudflared</code></td> </tr> <tr> <td>Restart Cloudflare tunnel</td> <td><code class="language-plaintext highlighter-rouge">sudo systemctl restart cloudflared</code></td> </tr> </tbody> </table> <hr/> <h2 id="architecture-overview">Architecture overview</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Phone / any device
        │
        │ HTTPS
        ▼
Cloudflare Tunnel
        │
        │ HTTP (local)
        ▼
Raspberry Pi
  ├── Docker: Audiobookshelf (:13378)
  ├── Docker: Home Assistant (:8123)  [optional]
  └── Storage: USB SSD / SD card
</code></pre></div></div> <hr/> <p><em>Built and documented from a real setup session. Total time from scratch to streaming: under an hour.</em></p>]]></content><author><name></name></author><category term="sample-posts"/><category term="hacking"/><summary type="html"><![CDATA[A complete guide to building a private audiobook server with Raspberry Pi, Audiobookshelf, and Cloudflare Tunnel.]]></summary></entry><entry><title type="html">why making a new personal website?</title><link href="https://carlossmwolff.github.io/blog/2025/new-site/" rel="alternate" type="text/html" title="why making a new personal website?"/><published>2025-01-22T15:00:00+00:00</published><updated>2025-01-22T15:00:00+00:00</updated><id>https://carlossmwolff.github.io/blog/2025/new-site</id><content type="html" xml:base="https://carlossmwolff.github.io/blog/2025/new-site/"><![CDATA[<p>I have (another) new personal website. Welcome to it!</p> <p>This is the first post on the <a href="/blog/">site’s blog</a>, so I guess it is a good place to talk about the reasons of bulding yet another personal website.</p> <p>My first website was <a href="https://web.archive.org/web/20170515000000*/www.carlossmwolff.org">www.carlossmwolff.org</a>, which I built during my PhD around 2015 and kept online for 3 years. Its focus was personal content (such as photography, geek blogs and stuff like that) rather than academic. It was a site built in Wordpress, and managing it never felt very pleasant to me: it was clunky and I never felt like I could really personalize it without resorting to external pluggins, templates and thing like that. Eventually I got tired of mantaining it and paying the hosting fees didn’t feel like worth it.</p> <p>Later, in 2020, I started a junior leader position at UAM, and I felt I needed an academic landing page. I used Weebly to <a href="https://carlossanchezmunoz.weebly.com/">create this one</a>. It was easy to create something with a nice look, plus it was free, but I missed having my own domain, and the editing experience was even worse than Wordpress. On top of that, navigation on that website felt very slow. In the end, I barely never remembered to update the page, probably because it was not fun doing so.</p> <p>At some point I heard about GitHub pages, and asking ChatGPT about it I ended up learning about Jekyll and the <a href="https://github.com/alshedivat/al-folio">al-folio theme</a> for academic websites. This is precisely what I needed! This approach has many advantages:</p> <ul> <li>The site is a GitHub repo made of plain text files which I can clone and inspect easily in my computer (instead of an obscure Wordpress site hosted in a random server)</li> <li>Pages are built with Markdown, a very simple markup language that anybody using e.g. Jupyter notebooks is familiar with.</li> <li>Hosting in Github is free, and adding your own domain for 10$ a year is very simple to do.</li> <li>You can easily add cron jobs as workflows running scripts with Github Actions.</li> <li>The end result is light, readable and really beautiful!</li> </ul> <p>Having a lightweight website stored in plain text files which I can complement with custom scripts is just what I wanted! For now, I am very happy with the result. I hope to write a follow-up post talking about the first tweaks I did to the theme and the custom scripts I have added, the main one being a python code that scrapes all my papers and citation info from Google Scholar and automatically builds a publication metrics summary in my <a href="/publications/">publications page</a>.</p>]]></content><author><name></name></author><category term="sample-posts"/><summary type="html"><![CDATA[I have (another) new personal website. Welcome to it!]]></summary></entry></feed>